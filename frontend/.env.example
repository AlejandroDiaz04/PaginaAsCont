# Vacío en producción (same-origin con PHP).
# En local: dejalo vacío y usá el proxy de vite.config (ver vite.config.example.ts),
# o poné la URL absoluta del PHP si no usás proxy:
# VITE_API_URL=http://localhost:8000
VITE_API_URL=
